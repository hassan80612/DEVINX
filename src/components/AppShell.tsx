'use client';
import Link from 'next/link';import {usePathname} from 'next/navigation';
const nav=[['/painel','Início'],['/rendas','Rendas'],['/gastos','Gastos'],['/metas','Metas'],['/mais','Mais']] as const;
export function AppShell({title,children}:{title:string;children:React.ReactNode}){const pathname=usePathname();const month=new Intl.DateTimeFormat('pt-BR',{month:'long',year:'numeric'}).format(new Date()).toUpperCase();return <main className="appShell"><header className="appHeader"><Link href="/painel" className="brand"><span className="mark">D</span><b>DEVINX</b></Link><span className="month">{month}</span></header><section className="appContent"><h1>{title}</h1>{children}</section><nav className="bottomNav">{nav.map(([href,label])=><Link href={href} key={href} className={pathname===href||pathname.startsWith(href+'/')?'active':''}>{label}</Link>)}</nav></main>}
