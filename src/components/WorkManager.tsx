'use client';

import {FormEvent,useEffect,useMemo,useState} from 'react';
import {createClient} from '@/lib/supabase/client';
import {hoursToGoal} from '@/domain/finance';

type Vehicle={id:string;name:string;energy_type:string;efficiency:number|null;unit_price_minor:number|null};
type Source={id:string;name:string;kind:'driver'|'delivery'};
type Session={gross_income_minor:number;energy_cost_minor:number;extra_work_cost_minor:number;distance_km:number;minutes_worked:number;worked_on:string;income_source_id:string|null};
type Goal={name:string;target_minor:number;basis:string;period:string};
const brl=(v:number)=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(v/100);
const minor=(raw:string)=>Math.round((Number(raw.replace(/\./g,'').replace(',','.'))||0)*100);
const isoDate=(d:Date)=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
function periodStart(period:string){const d=new Date();if(period==='daily')return isoDate(d);if(period==='weekly'){const day=(d.getDay()+6)%7;d.setDate(d.getDate()-day);return isoDate(d)}d.setDate(1);return isoDate(d)}

export function WorkManager(){
  const[vehicle,setVehicle]=useState<Vehicle|null>(null);const[sources,setSources]=useState<Source[]>([]);const[sessions,setSessions]=useState<Session[]>([]);const[goal,setGoal]=useState<Goal|null>(null);const[name,setName]=useState('Meu veículo');const[energy,setEnergy]=useState('gasoline');const[efficiency,setEfficiency]=useState('10');const[unitPrice,setUnitPrice]=useState('6');const[sourceId,setSourceId]=useState('');const[newSource,setNewSource]=useState('');const[newSourceKind,setNewSourceKind]=useState<'driver'|'delivery'>('driver');const[gross,setGross]=useState('');const[km,setKm]=useState('');const[hours,setHours]=useState('');const[extra,setExtra]=useState('0');const[notice,setNotice]=useState('');
  async function load(){const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user){location.href='/entrar';return}const since=new Date();since.setDate(since.getDate()-30);const[v,src,ss,g]=await Promise.all([s.from('vehicles').select('id,name,energy_type,efficiency,unit_price_minor').eq('user_id',user.id).eq('is_default',true).limit(1),s.from('income_sources').select('id,name,kind').eq('user_id',user.id).in('kind',['driver','delivery']).eq('is_active',true).order('created_at'),s.from('work_sessions').select('gross_income_minor,energy_cost_minor,extra_work_cost_minor,distance_km,minutes_worked,worked_on,income_source_id').eq('user_id',user.id).gte('worked_on',isoDate(since)).order('worked_on',{ascending:false}),s.from('goals').select('name,target_minor,basis,period').eq('user_id',user.id).eq('is_active',true).in('basis',['gross','operational_net']).limit(1)]);const sourceRows=(src.data||[]) as Source[];setVehicle((v.data?.[0] as Vehicle)||null);setSources(sourceRows);setSessions((ss.data||[]) as Session[]);setGoal((g.data?.[0] as Goal)||null);setSourceId(current=>current||sourceRows[0]?.id||'')}
  useEffect(()=>{load()},[]);
  const stats=useMemo(()=>{const grossIncome=sessions.reduce((a,b)=>a+Number(b.gross_income_minor),0);const energyCost=sessions.reduce((a,b)=>a+Number(b.energy_cost_minor),0);const extraCost=sessions.reduce((a,b)=>a+Number(b.extra_work_cost_minor),0);const distance=sessions.reduce((a,b)=>a+Number(b.distance_km),0);const workedHours=sessions.reduce((a,b)=>a+Number(b.minutes_worked),0)/60;const operationalCost=energyCost+extraCost;const net=grossIncome-operationalCost;return{gross:grossIncome,energyCost,extraCost,operationalCost,distance,h:workedHours,net,grossPerHour:workedHours>0?grossIncome/workedHours:0,netPerHour:workedHours>0?net/workedHours:0,perKm:distance>0?net/distance:0}},[sessions]);
  const sourceStats=useMemo(()=>sources.map(source=>{const rows=sessions.filter(s=>s.income_source_id===source.id);const h=rows.reduce((a,b)=>a+Number(b.minutes_worked),0)/60;const grossIncome=rows.reduce((a,b)=>a+Number(b.gross_income_minor),0);const cost=rows.reduce((a,b)=>a+Number(b.energy_cost_minor)+Number(b.extra_work_cost_minor),0);const net=grossIncome-cost;return{id:source.id,name:source.name,h,netPerHour:h>0?net/h:0,grossPerHour:h>0?grossIncome/h:0}}).filter(x=>x.h>0),[sources,sessions]);
  const goalForecast=useMemo(()=>{if(!goal)return null;const start=periodStart(goal.period);const current=sessions.filter(s=>s.worked_on>=start).reduce((sum,s)=>sum+(goal.basis==='gross'?Number(s.gross_income_minor):Number(s.gross_income_minor)-Number(s.energy_cost_minor)-Number(s.extra_work_cost_minor)),0);const remaining=Math.max(0,Number(goal.target_minor)-current);const historyReady=sessions.length>=3&&stats.h>=3;const rate=goal.basis==='gross'?stats.grossPerHour:stats.netPerHour;return{current,remaining,historyReady,hours:historyReady?hoursToGoal(remaining,rate):null}},[goal,sessions,stats]);
  async function saveVehicle(e:FormEvent){e.preventDefault();const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user)return;await s.from('vehicles').update({is_default:false}).eq('user_id',user.id).eq('is_default',true);const{error}=await s.from('vehicles').insert({user_id:user.id,name,energy_type:energy,efficiency:energy==='human'?null:Number(efficiency),unit_price_minor:energy==='human'?0:minor(unitPrice),is_default:true});if(error){setNotice('Não foi possível salvar o veículo.');return}setNotice('Veículo salvo.');await load()}
  async function addSource(){const value=newSource.trim();if(!value)return;const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user)return;const{error}=await s.from('income_sources').upsert({user_id:user.id,kind:newSourceKind,name:value},{onConflict:'user_id,kind,name',ignoreDuplicates:true});if(error){setNotice('Não foi possível adicionar a fonte.');return}setNewSource('');await load()}
  async function saveSession(e:FormEvent){e.preventDefault();if(!vehicle){setNotice('Configure seu veículo primeiro.');return}const distance=Number(km.replace(',','.'))||0;const workedHours=Number(hours.replace(',','.'))||0;if(workedHours<=0){setNotice('Informe as horas trabalhadas.');return}const efficiencyValue=Number(vehicle.efficiency)||0;const energyCost=vehicle.energy_type==='human'?0:efficiencyValue>0?Math.round((distance/efficiencyValue)*Number(vehicle.unit_price_minor||0)):0;const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user)return;const{error}=await s.from('work_sessions').insert({user_id:user.id,vehicle_id:vehicle.id,income_source_id:sourceId||null,worked_on:isoDate(new Date()),gross_income_minor:minor(gross),energy_cost_minor:energyCost,extra_work_cost_minor:minor(extra),distance_km:distance,minutes_worked:Math.round(workedHours*60)});if(error){setNotice('Não foi possível salvar a jornada.');return}setGross('');setKm('');setHours('');setExtra('0');setNotice('Jornada registrada.');await load()}
  return <>{!vehicle?<section className="panel"><h2>Configure seu veículo</h2><p className="lead">Isso é feito uma vez. O Devinx calcula combustível ou energia automaticamente.</p><form className="entryForm" onSubmit={saveVehicle}><label>Nome<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Tipo<select value={energy} onChange={e=>setEnergy(e.target.value)}><option value="gasoline">Gasolina</option><option value="ethanol">Etanol</option><option value="diesel">Diesel</option><option value="hybrid">Híbrido</option><option value="electric">Elétrico</option><option value="human">Bicicleta</option></select></label>{energy!=='human'&&<><label>{energy==='electric'?'Km por kWh':'Km por litro'}<input value={efficiency} onChange={e=>setEfficiency(e.target.value)} inputMode="decimal"/></label><label>{energy==='electric'?'Preço do kWh':'Preço por litro'}<input value={unitPrice} onChange={e=>setUnitPrice(e.target.value)} inputMode="decimal"/></label></>}<button className="primary">Salvar veículo</button></form></section>:<><div className="metricGrid"><article><small>Líquido / hora</small><b>{stats.h?brl(Math.round(stats.netPerHour)):'—'}</b></article><article><small>Líquido / km</small><b>{stats.distance?brl(Math.round(stats.perKm)):'—'}</b></article><article><small>Combustível / energia</small><b>{brl(stats.energyCost)}</b></article><article><small>Líquido operacional</small><b>{brl(stats.net)}</b></article></div>{goal&&goalForecast&&<section className="panel"><h2>{goal.name}</h2><p className="lead">{brl(goalForecast.current)} de {brl(Number(goal.target_minor))} neste período.</p>{goalForecast.remaining===0?<div className="goalForecast good"><b>Meta alcançada</b><span>Você já atingiu essa meta.</span></div>:goalForecast.historyReady&&goalForecast.hours!==null?<div className="goalForecast"><b>Estimativa: {goalForecast.hours.toFixed(1)} h</b><span>Baseada apenas no seu próprio histórico dos últimos 30 dias.</span></div>:<div className="note">Registre pelo menos 3 jornadas e 3 horas de trabalho para o Devinx estimar quanto falta trabalhar sem inventar médias.</div>}</section>}<section className="panel"><h2>Registrar jornada</h2><p className="lead">Veículo: {vehicle.name}</p>{sources.length>0&&<label className="standaloneLabel">Fonte de renda<select value={sourceId} onChange={e=>setSourceId(e.target.value)}>{sources.map(source=><option value={source.id} key={source.id}>{source.name}</option>)}</select></label>}<form className="entryForm" onSubmit={saveSession}><label>Ganhos<input required value={gross} onChange={e=>setGross(e.target.value)} placeholder="0,00" inputMode="decimal"/></label><label>Quilômetros<input required value={km} onChange={e=>setKm(e.target.value)} placeholder="0" inputMode="decimal"/></label><label>Horas trabalhadas<input required value={hours} onChange={e=>setHours(e.target.value)} placeholder="0" inputMode="decimal"/></label><label>Outros custos do trabalho<input value={extra} onChange={e=>setExtra(e.target.value)} placeholder="0,00" inputMode="decimal"/></label><button className="primary">Salvar jornada</button></form><div className="inlineAdd sourceAdd"><select value={newSourceKind} onChange={e=>setNewSourceKind(e.target.value as 'driver'|'delivery')}><option value="driver">Motorista</option><option value="delivery">Entregador</option></select><input value={newSource} onChange={e=>setNewSource(e.target.value)} placeholder="Adicionar outra fonte"/><button type="button" className="secondary" onClick={addSource}>Adicionar</button></div></section>{sourceStats.length>=2&&<section className="panel"><h2>Comparação pelas suas fontes</h2><p className="lead">Somente com os seus próprios registros dos últimos 30 dias.</p><div className="sourceComparison">{sourceStats.map(source=><article key={source.id}><b>{source.name}</b><strong>{brl(Math.round(source.netPerHour))}/h</strong><small>{source.h.toFixed(1)} h registradas</small></article>)}</div></section>}<section className="note">Almoço, mercado, aluguel e outros gastos pessoais não entram no cálculo da produtividade. Só combustível/energia e custos operacionais do trabalho.</section></>}{notice&&<div className="authMessage">{notice}</div>}</>}
