'use client';
import {useEffect,useMemo,useState} from 'react';import {createClient} from '@/lib/supabase/client';
type Tx={type:'income'|'expense';amount_minor:number;is_avoidable:boolean};type Work={gross_income_minor:number;energy_cost_minor:number};type Goal={name:string;target_minor:number;basis:string};type Bill={id:string;amount_minor:number};type Payment={recurring_bill_id:string};type Installment={amount_minor:number;paid_at:string|null};
const brl=(v:number)=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(v/100);
export function DashboardOverview(){const[tx,setTx]=useState<Tx[]>([]);const[work,setWork]=useState<Work[]>([]);const[goals,setGoals]=useState<Goal[]>([]);const[bills,setBills]=useState<Bill[]>([]);const[payments,setPayments]=useState<Payment[]>([]);const[installments,setInstallments]=useState<Installment[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{(async()=>{const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user){location.href='/entrar';return}const n=new Date();const start=`${n.getFullYear()}-${String(n.getMonth()+1).padStart(2,'0')}-01`;const endDate=new Date(n.getFullYear(),n.getMonth()+1,0);const end=`${endDate.getFullYear()}-${String(endDate.getMonth()+1).padStart(2,'0')}-${String(endDate.getDate()).padStart(2,'0')}`;const[t,w,g,b,p,i]=await Promise.all([s.from('transactions').select('type,amount_minor,is_avoidable').eq('user_id',user.id).gte('occurred_on',start).lte('occurred_on',end),s.from('work_sessions').select('gross_income_minor,energy_cost_minor').eq('user_id',user.id).gte('worked_on',start).lte('worked_on',end),s.from('goals').select('name,target_minor,basis').eq('user_id',user.id).eq('is_active',true).limit(1),s.from('recurring_bills').select('id,amount_minor').eq('user_id',user.id).eq('is_active',true),s.from('recurring_bill_payments').select('recurring_bill_id').eq('user_id',user.id).eq('due_month',start),s.from('card_installments').select('amount_minor,paid_at').eq('user_id',user.id).eq('billing_month',start)]);setTx((t.data||[]) as Tx[]);setWork((w.data||[]) as Work[]);setGoals((g.data||[]) as Goal[]);setBills((b.data||[]) as Bill[]);setPayments((p.data||[]) as Payment[]);setInstallments((i.data||[]) as Installment[]);setLoading(false)})()},[]);const numbers=useMemo(()=>{const manualIncome=tx.filter(x=>x.type==='income').reduce((a,b)=>a+Number(b.amount_minor),0);const manualExpense=tx.filter(x=>x.type==='expense').reduce((a,b)=>a+Number(b.amount_minor),0);const workGross=work.reduce((a,b)=>a+Number(b.gross_income_minor),0);const workCost=work.reduce((a,b)=>a+Number(b.energy_cost_minor),0);const cardExpense=installments.reduce((a,b)=>a+Number(b.amount_minor),0);const paidBillIds=new Set(payments.map(p=>p.recurring_bill_id));const recurringPaid=bills.filter(b=>paidBillIds.has(b.id)).reduce((a,b)=>a+Number(b.amount_minor),0);const recurringUnpaid=bills.filter(b=>!paidBillIds.has(b.id)).reduce((a,b)=>a+Number(b.amount_minor),0);const unpaidCards=installments.filter(i=>!i.paid_at).reduce((a,b)=>a+Number(b.amount_minor),0);const income=manualIncome+workGross;const expense=manualExpense+workCost+cardExpense+recurringPaid;const avoidable=tx.filter(x=>x.type==='expense'&&x.is_avoidable).reduce((a,b)=>a+Number(b.amount_minor),0);const toPay=recurringUnpaid+unpaidCards;return{income,expense,balance:income-expense,toPay,projected:income-expense-toPay,avoidable,workGross,workCost}},[tx,work,bills,payments,installments]);const goal=goals[0];const goalBase=goal?.basis==='operational_net'?numbers.workGross-numbers.workCost:goal?.basis==='savings'?Math.max(0,numbers.balance):numbers.income;const progress=goal?Math.min(100,Math.round((goalBase/Number(goal.target_minor))*100)):0;if(loading)return <section className="panel"><b>Carregando seus números...</b></section>;return <><section className="summaryHero"><small>SALDO PREVISTO</small><strong>{brl(numbers.projected)}</strong><span>já considerando compromissos ainda a pagar</span></section><div className="metricGrid"><article><small>Entrou</small><b>{brl(numbers.income)}</b></article><article><small>Gastou</small><b>{brl(numbers.expense)}</b></article><article><small>Ainda a pagar</small><b>{brl(numbers.toPay)}</b></article><article><small>Evitável</small><b>{brl(numbers.avoidable)}</b></article></div>{goal&&<section className="panel"><h2>{goal.name}</h2><div className="bar"><i style={{width:`${progress}%`}}/></div><p className="lead">{progress}% da meta · {brl(goalBase)} de {brl(Number(goal.target_minor))}</p></section>}<section className="panel"><h2>Registro rápido</h2><div className="actionGrid"><a href="/rendas">+ Entrada</a><a href="/gastos">- Gasto</a><a href="/trabalho">+ Trabalho</a></div></section><section className="panel"><h2>Próximos compromissos</h2><p className="lead">Você tem {brl(numbers.toPay)} entre contas recorrentes e faturas ainda não quitadas neste mês.</p></section>{numbers.income===0&&numbers.expense===0&&<section className="empty"><b>Seu painel começa aqui</b><p>Registre sua primeira entrada, gasto ou jornada de trabalho.</p><a href="/onboarding" className="primary">Configurar minha vida financeira</a></section>}</>}
